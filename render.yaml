services:
  - type: web
    name: geoai-hydraulic-dashboard
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn api:app
    healthCheckPath: /api/health
    envVars:
      - key: CORS_ORIGINS
        value: "*"
